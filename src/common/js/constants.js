/*
 * pwix:ssr/src/common/js/constants.js
 */

SSR.C = {

    // verbosity level
    Verbose: {
        NONE:           0,
        CONFIGURE:      0x01 <<  0,
        COLLECTIONS:    0x01 <<  1
    }
};

// non exported

ORIGID_BOT = 'botCrawler';
ORIGID_PRERENDER = 'prerender';
